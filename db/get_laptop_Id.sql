select * from products
where size = ${productSize} and color = ${productColor} and storage = ${productStorage} and name = ${productName} and panda_care = ${pandaCare} and type = ${productType} and ram = ${productRam} and processor = ${productProcessor};